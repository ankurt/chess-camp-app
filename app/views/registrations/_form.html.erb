<%= simple_form_for(@registration) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
   <%= f.label :camp_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
